# Base URL of the @study-hack/api server. Exposed to the browser (client
# components fetch uploads and load page images directly from the API).
NEXT_PUBLIC_API_URL=http://localhost:4000

# Set to 1 to show the Google sign-in button; 0 disables it.
NEXT_PUBLIC_GOOGLE_ENABLED=0
