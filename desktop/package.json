{
  "name": "study-hack-desktop",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Electron shell for study-hack. Starts the packaged Next server and points a window at it.",
  "author": "study_hack",
  "license": "MIT",
  "main": "main.js",
  "scripts": {
    "payload": "node scripts/build-payload.mjs",
    "dist": "electron-builder --config electron-builder.yml"
  },
  "devDependencies": {
    "electron": "^44.1.1",
    "electron-builder": "^26.0.12"
  }
}
